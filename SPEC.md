# SPEC: 将 xv6-riscv 代码注释翻译为中文

## 我要做什么
将 xv6-riscv 内核源码中的所有 **代码注释（`//` 与 `/* */`）** 翻译成中文。
- 仅修改注释内容，不改变任何代码（包括汇编指令、C 语法、宏定义、变量名等）。
- 保留原有注释的结构、缩进与位置。
- 对文件系统、进程管理、内存管理等所有源码目录均需翻译。

## 边界在哪
- **仅限注释**：严格限定在 `// ...` 和 `/* ... */` 中的自然语言文本。代码、字符串常量、文档注释中的代码示例均不翻译。
- **不修改代码**：绝对禁止修改任何非注释的字符。哪怕是一个空格或换行，若不属于注释部分就不能动。
- **分批次进行**：不要一次性修改所有文件。每次只处理一个目录或一个逻辑模块（如先处理 `kernel/`，再处理 `user/`），并在每次修改后立即验证编译。
- **保持格式**：保留 Doxygen 风格的 `@param`、`@return` 等标签，仅翻译其后的说明文字。

## 怎样算做完
1. 所有 `*.c`、`*.h`、`*.S` 文件中的英文注释均已替换为中文。
2. 在每次修改一个目录后，立即执行 `make qemu` 确保编译通过且系统正常启动。
3. 最终提交前，在项目根目录运行 `make qemu` 并通过，且 `git diff` 仅显示注释内容的变更。
4. 翻译后的中文语句通顺，准确传达原注释的技术含义。
5. 所有修改记录到 `CLAUDE_MEMORY.md`，说明处理了哪些文件、遇到的问题及解决方案。

## 执行协议 (必须遵守)
- **动手前**：每处理一个文件前，先用一句话报告文件名及计划修改的注释数量，经我确认后再动手。
- **写入后**：每修改完一个文件，必须自动运行 `make qemu`（通过 Hook 或手动执行），编译通过后方可继续下一个文件。
- **遵守行为准则**：严格遵守全局配置 `~/.claude/CLAUDE.md` 中的 Karpathy Guidelines —— 先思考再编码、简洁至上、精准修改、目标驱动。
